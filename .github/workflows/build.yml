name: Build

on:
  push:
  pull_request:

jobs:
  swift-build:
    runs-on: macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build release
        run: swift build -c release
